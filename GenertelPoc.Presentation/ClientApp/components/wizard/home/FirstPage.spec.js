import { expect } from 'chai'
import { mount } from '@vue/test-utils'
import FirstPage from './FirstPage.vue'

describe('FirstPage.vue', () => {
    it('has a root element with class foo', () => {
        // const wrapper = mount(FirstPage, {
        //     propsData: {
        //         pageId: 1
        //     }
        // })

        // const textInput = wrapper.find('text-input')
        // //textInput.('click')
        // expect(wrapper.vm.pageId).toBe(1)
        // expect(wrapper.is('.foo')).to.equal(true)
        // expect(wrapper.vm.$data.msg).to.equal('Welcome to Your Vue.js App');
    })
})